import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Lab4Firebase',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
